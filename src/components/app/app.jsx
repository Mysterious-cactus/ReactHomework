import { Layout } from "../layout/component";
import { RestaurantContent } from "../restaurantContent/component";

export const App = () => {
    return (
        <Layout>
            <RestaurantContent />
        </Layout>
    );
};
